apiVersion: v1
kind : PersistentVolume
metadata:
  name: pv-test
spec:
  capacity:
    storage: 5Gi
    accessModes:
      - ReadWriteOnce
    persistentVolumeReclaimPolicy: Recycle
    empty: {}
       


